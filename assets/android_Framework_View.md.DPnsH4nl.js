import{_ as e,c as i,o as t,al as c}from"./chunks/framework.GtCa91sV.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"android/Framework/View.md","filePath":"android/Framework/View.md","lastUpdated":1764176925000}'),d={name:"android/Framework/View.md"};function n(r,o,l,s,a,g){return t(),i("div",null,[...o[0]||(o[0]=[c('<p>好的，Android 的 <code>View</code> 体系是面试中另一个超级核心的主题，尤其对于应用开发岗位。考察点可以从 View 的基础知识、绘制流程、事件分发，深入到自定义 View 和性能优化。</p><p>下面我将为你系统地梳理关于 <code>View</code> 所能考察的所有知识点。</p><hr><h3 id="一、view-基础-view-basics" tabindex="-1">一、View 基础 (View Basics) <a class="header-anchor" href="#一、view-基础-view-basics" aria-label="Permalink to “一、View 基础 (View Basics)”">​</a></h3><p>这部分是理解 View 的起点，通常用于考察基础是否扎实。</p><ol><li><p><strong>View 的坐标体系</strong></p><ul><li><strong>相对坐标</strong>：<code>getTop()</code>, <code>getBottom()</code>, <code>getLeft()</code>, <code>GetRight()</code>。这些是 View <strong>相对于其父容器</strong>的坐标。</li><li><strong>绝对坐标</strong>：<code>getX()</code>, <code>getY()</code>。表示的是触摸点相对于 View 左上角的坐标。</li><li><strong>Raw 坐标</strong>：<code>getRawX()</code>, <code>getRawY()</code>。表示的是触摸点相对于<strong>整个屏幕</strong>左上角的坐标。</li></ul></li><li><p><strong>View 的滑动</strong></p><ul><li><strong>layout()</strong>：通过改变 <code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 来重新放置 View。</li><li><strong>offsetLeftAndRight() / offsetTopAndBottom()</strong>：对 View 进行偏移。</li><li><strong>LayoutParams</strong>：改变 View 的布局参数（如 <code>MarginLayoutParams</code>）。</li><li><strong>动画</strong>：View 动画（补间动画）和属性动画（Animator）。注意 View 动画并不会改变 View 的真是位置，而属性动画（Android 3.0+）会。</li><li><strong>scrollTo / scrollBy</strong>：<strong>重要区别</strong>：这两个方法移动的是 <strong>View 的“内容”</strong> 或者说是 <strong>“视口”</strong>，而不是 View 本身的位置。常用于 <code>ViewGroup</code>（如 <code>ScrollView</code>）。理解 <code>mScrollX</code> 和 <code>mScrollY</code> 的正负值。</li></ul></li><li><p><strong>View 的生命周期</strong></p><ul><li>指的是一个 View 从被创建（<code>onAttachedToWindow</code>）到被销毁（<code>onDetachedFromWindow</code>）的过程。</li><li>关键方法：<code>onAttachedToWindow</code> -&gt; <code>onMeasure</code> -&gt; <code>onLayout</code> -&gt; <code>onDraw</code> -&gt; ... -&gt; <code>onDetachedFromWindow</code>。</li></ul></li></ol><hr><h3 id="二、view-的绘制流程-view-drawing-process" tabindex="-1">二、View 的绘制流程 (View Drawing Process) <a class="header-anchor" href="#二、view-的绘制流程-view-drawing-process" aria-label="Permalink to “二、View 的绘制流程 (View Drawing Process)”">​</a></h3><p>这是 View 体系的重中之重，必须深刻理解。整个过程始于 <code>ViewRootImpl</code> 的 <code>performTraversals()</code> 方法。</p><ol><li><p><strong>三大流程：Measure -&gt; Layout -&gt; Draw</strong></p><ul><li><strong>Measure (测量)</strong>：<strong>测量 View 的宽高</strong>。 <ul><li><code>onMeasure(int widthMeasureSpec, int heightMeasureSpec)</code>：View 在这里确定自己的尺寸。</li><li><strong>MeasureSpec</strong>：一个 32 位 int 值，高 2 位是 <strong>Mode</strong>，低 30 位是 <strong>Size</strong>。这是理解测量的关键。 <ul><li><code>EXACTLY</code>：精确值模式，对应 <code>layout_width=&quot;100dp&quot;</code> 或 <code>match_parent</code>。</li><li><code>AT_MOST</code>：最大值模式，对应 <code>wrap_content</code>。</li><li><code>UNSPECIFIED</code>：未指定，想多大就多大，常用于 <code>ScrollView</code> 等可滑动的容器。</li></ul></li><li><strong>ViewGroup 的测量</strong>：<code>ViewGroup</code>（如 <code>LinearLayout</code>）需要先测量所有子 View，然后才能确定自己的尺寸。</li></ul></li><li><strong>Layout (布局)</strong>：<strong>确定 View 在父容器中的位置</strong>。 <ul><li><code>onLayout(boolean changed, int l, int t, int r, int b)</code>：<code>ViewGroup</code> 必须重写此方法，来安排子 View 的位置（调用子 View 的 <code>layout</code> 方法）。</li></ul></li><li><strong>Draw (绘制)</strong>：<strong>将 View 绘制到屏幕上</strong>。 <ul><li><code>onDraw(Canvas canvas)</code>：View 在这里进行具体的绘制操作（画背景、画内容）。</li><li><code>ViewGroup</code> 默认不会调用 <code>onDraw</code>，除非调用了 <code>setWillNotDraw(false)</code>。</li><li><strong>绘制顺序</strong>：背景 -&gt; 自身内容（<code>onDraw</code>） -&gt; 子 View（<code>dispatchDraw</code>） -&gt; 装饰（如滚动条）。</li></ul></li></ul></li><li><p><strong>自定义 View 时如何处理 wrap_content 和 padding？</strong></p><ul><li><strong>经典问题</strong>：如果不处理，自定义 View 在设置为 <code>wrap_content</code> 时会和 <code>match_parent</code> 效果一样。</li><li><strong>解决方案</strong>：在 <code>onMeasure</code> 中，当 <code>MeasureSpec</code> 的 mode 是 <code>AT_MOST</code> 时，计算一个默认的或者内容所需的最小尺寸，而不是直接使用父容器给出的 <code>size</code>。</li><li><strong>处理 padding</strong>：在 <code>onDraw</code> 和计算尺寸时，要考虑到 <code>getPaddingLeft()</code>, <code>getPaddingTop()</code> 等值。</li></ul></li></ol><hr><h3 id="三、view-的事件分发机制-event-distribution" tabindex="-1">三、View 的事件分发机制 (Event Distribution) <a class="header-anchor" href="#三、view-的事件分发机制-event-distribution" aria-label="Permalink to “三、View 的事件分发机制 (Event Distribution)”">​</a></h3><p>另一个超级核心的考点，理解“责任链模式”在 Android 中的应用。</p><ol><li><p><strong>三个核心方法</strong></p><ul><li><code>dispatchTouchEvent(MotionEvent ev)</code>：<strong>进行事件分发</strong>。返回值表示是否消耗了当前事件。</li><li><code>onInterceptTouchEvent(MotionEvent ev)</code>：<strong>判断是否拦截某个事件</strong>。只有 <code>ViewGroup</code> 有此方法。</li><li><code>onTouchEvent(MotionEvent ev)</code>：<strong>处理点击事件</strong>。返回值表示是否消耗了当前事件。</li></ul></li><li><p><strong>事件分发流程 (就像一个U型管)</strong></p><ol><li><strong>DOWN 事件开始</strong>：事件从 <code>Activity.dispatchTouchEvent</code> 开始，传递给 <code>Window</code>，再传递给顶级 <code>ViewGroup</code>（通常是 <code>DecorView</code>）。</li><li><strong>向下传递 (Intercept)</strong>：事件从上到下传递。每一层的 <code>ViewGroup</code> 会先调用 <code>onInterceptTouchEvent</code> 判断是否拦截。如果拦截，则事件不再向下，转而交给该 <code>ViewGroup</code> 的 <code>onTouchEvent</code> 处理。</li><li><strong>向上传递 (Handle)</strong>：如果没有任何 <code>ViewGroup</code> 拦截，事件最终会传递到最底层的 <code>View</code> 的 <code>onTouchEvent</code> 方法。</li><li><strong>事件消耗</strong>：如果某个 View 的 <code>onTouchEvent</code> 返回 <code>true</code>，表示它<strong>消耗</strong>了这个事件。那么后续的 <strong>MOVE、UP</strong> 等事件序列将直接交给它处理，不会再经历完整的向下传递和判断拦截过程。</li><li><strong>事件回溯</strong>：如果所有子 View 和 <code>ViewGroup</code> 都不消耗事件（<code>onTouchEvent</code> 返回 <code>false</code>），事件会层层向上回溯，最终交给 <code>Activity.onTouchEvent</code> 处理。</li></ol></li><li><p><strong>onTouchListener 与 onTouchEvent 的优先级</strong></p><ul><li>如果一个 View 设置了 <code>OnTouchListener</code>，那么会先执行 <code>listener.onTouch()</code>。</li><li>如果 <code>onTouch()</code> 返回 <code>true</code>，则 <code>onTouchEvent</code> 将<strong>不会被执行</strong>。</li><li>否则，才会继续执行 <code>onTouchEvent</code>。在 <code>onTouchEvent</code> 中，如果 View 是 <code>CLICKABLE</code> 的，最后会执行 <code>OnClickListener</code>。</li></ul></li><li><p><strong>滑动冲突</strong></p><ul><li><strong>场景</strong>：内外两层控件都可以滑动，如 <code>ScrollView</code> 里面套 <code>ListView</code>，或者 <code>ViewPager</code> 里面套 <code>ListView</code>。</li><li><strong>解决思路</strong>： <ul><li><strong>外部拦截法</strong>：重写<strong>父容器</strong>的 <code>onInterceptTouchEvent</code>。在 DOWN 事件时不拦截，在 MOVE 事件时根据条件判断是否需要拦截。<strong>（推荐）</strong></li><li><strong>内部拦截法</strong>：重写<strong>子 View</strong> 的 <code>dispatchTouchEvent</code>，通过 <code>requestDisallowInterceptTouchEvent</code> 方法来请求父容器是否拦截。</li></ul></li></ul></li></ol><hr><h3 id="四、自定义-view-custom-view" tabindex="-1">四、自定义 View (Custom View) <a class="header-anchor" href="#四、自定义-view-custom-view" aria-label="Permalink to “四、自定义 View (Custom View)”">​</a></h3><p>考察实践能力和对原理的理解深度。</p><ol><li><p><strong>分类</strong></p><ul><li><strong>继承 View</strong>：需要自己处理 <code>onMeasure</code> 和 <code>onDraw</code>。用于实现不规则形状的 View。</li><li><strong>继承 ViewGroup</strong>：需要自己处理 <code>onMeasure</code> 和 <code>onLayout</code>。用于实现自定义布局。</li><li><strong>继承特定 View（如 TextView）</strong>：在现有控件基础上进行功能扩展。</li></ul></li><li><p><strong>关键点</strong></p><ul><li><strong>支持 wrap_content 和 padding</strong>（如前所述）。</li><li><strong>处理自定义属性</strong>：在 <code>res/values/attrs.xml</code> 中定义属性，在构造方法中通过 <code>TypedArray</code> 获取。</li><li><strong>考虑性能</strong>：避免在 <code>onDraw</code> 中创建新对象（如 <code>Paint</code>, <code>Path</code>），应提前初始化。</li></ul></li><li><p><strong>Canvas 和 Paint 的常用操作</strong></p><ul><li><code>Canvas</code>：<code>drawColor</code>, <code>drawCircle</code>, <code>drawRect</code>, <code>drawPath</code>, <code>drawText</code>, <code>drawBitmap</code> 等。</li><li><code>Paint</code>：设置颜色、样式（<code>FILL</code>, <code>STROKE</code>）、抗锯齿、文字大小等。</li></ul></li></ol><hr><h3 id="五、面试经典问题" tabindex="-1">五、面试经典问题 <a class="header-anchor" href="#五、面试经典问题" aria-label="Permalink to “五、面试经典问题”">​</a></h3><ol><li><p><strong>View 的测量流程中，match_parent 和 wrap_content 有什么区别？</strong></p><ul><li>答：它们对应的 <code>MeasureSpec</code> 的 Mode 不同。<code>match_parent</code> 是 <code>EXACTLY</code>，子 View 必须使用父容器给出的尺寸；<code>wrap_content</code> 是 <code>AT_MOST</code>，子 View 的尺寸不能超过父容器给出的尺寸。</li></ul></li><li><p><strong>requestLayout() 和 invalidate() 的区别？</strong></p><ul><li><code>invalidate()</code>：只会触发 <strong><code>onDraw</code></strong>，用于刷新视图。必须在 UI 线程调用。</li><li><code>requestLayout()</code>：会触发完整的 <strong><code>measure</code> -&gt; <code>layout</code></strong> 流程，不一定会触发 <code>draw</code>。当 View 的尺寸或位置可能发生变化时调用。</li></ul></li><li><p><strong>postInvalidate() 和 invalidate() 的区别？</strong></p><ul><li><code>invalidate()</code> 必须在 UI 线程调用。</li><li><code>postInvalidate()</code> 可以在<strong>非 UI 线程</strong>中调用，它内部通过 <code>Handler</code> 将重绘请求发送到 UI 线程。</li></ul></li><li><p><strong>一个 MotionEvent 产生后，它的传递流程是怎样的？</strong></p><ul><li>答：Activity -&gt; Window -&gt; DecorView -&gt; 层层向下传递的 ViewGroup -&gt; 最终的子 View。如果没人处理，再层层向上回溯到 Activity。</li></ul></li><li><p><strong>如何解决滑动冲突？你一般采用哪种方案，为什么？</strong></p><ul><li>答：一般采用<strong>外部拦截法</strong>，因为它符合事件分发的逻辑，由父容器统一管理，逻辑更清晰，子 View 也无需关心。</li></ul></li></ol><h3 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h3><p>要应对关于 <code>View</code> 的所有考察，你需要：</p><ol><li><strong>能说清流程</strong>：清晰地描述 Measure、Layout、Draw 三大流程和事件分发流程。</li><li><strong>能解释细节</strong>：理解 <code>MeasureSpec</code>、<code>onLayout</code> 的作用、<code>onTouch</code> 和 <code>onClick</code> 的优先级等。</li><li><strong>能解决冲突</strong>：掌握滑动冲突的常见场景和解决方案。</li><li><strong>能动手实践</strong>：了解自定义 View 的分类、注意事项和关键步骤。</li><li><strong>能回答经典</strong>：对 <code>requestLayout/invalidate</code> 区别等问题对答如流。</li></ol><p>把这些知识点串联起来，你就构建了一个完整的 Android View 知识体系。</p>',25)])])}const p=e(d,[["render",n]]);export{u as __pageData,p as default};
